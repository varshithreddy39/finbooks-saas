// Production API URL — update this when you deploy the backend
// For local development, leave as empty string (api.js will use localhost:8001)
// For production: window.API_BASE_URL = 'https://your-backend.railway.app/api';
window.API_BASE_URL = '';
